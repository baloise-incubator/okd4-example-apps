apiVersion: v1
kind: Service
metadata:  
  name: webserver-service
spec:
  selector:
    app: apache
  ports:
    - protocol: TCP
      port: {{ .values.service.port }}
      targetPort: {{ .values.service.targetPort }}
  type: ClusterIP
