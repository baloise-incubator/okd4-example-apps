apiVersion: v1
kind: Service
metadata:
  name: db-service
spec:
  selector:
    app: database
  ports:
    - protocol: TCP
      port: {{ .Values.service.db-port }}
      targetPort: {{ .Values.service.db-targetPort }}
  type: ClusterIP

